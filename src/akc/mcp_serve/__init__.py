"""AKC as an MCP server (read-only tools for hosts / IDEs).

Install the optional ``mcp`` dependency (``uv sync --extra mcp-serve``) and run
``akc mcp serve``.
"""

from __future__ import annotations

__all__: list[str] = []
